import React from "react";
import HeroArea from "../HeroArea/HeroArea";

const Home = () => {
    return (
        <div>
            <HeroArea></HeroArea>
        </div>
    );
};

export default Home;
